package com.yhh.datastracture;

public interface List_01 {
    //从表头位置添加一个元素
    void push(int value);
    //从表头位置删除一个元素
    int pop();
    //找出第一个元素
    int peek();
    int size();
    void print();
}
